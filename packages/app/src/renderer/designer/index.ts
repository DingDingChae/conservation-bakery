/** The cake designer panel — see `panel.ts` for the mounted surface and `logic.ts`
 * for the pure logic (reference kitchen, presets, elevation geometry, register-aware
 * problem descriptions) it is built from. */
export { designerPanel } from './panel.js';
